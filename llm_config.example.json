{
    "_comment_1": "LLM 配置模板 — 复制为 llm_config.json 后填入真实值。不会提交到 git。",
    "_comment_2": "字段优先级：环境变量 > llm_config.json。环境变量直接覆盖本文件任何字段。",

    "_comment_protocol": "protocol ∈ { openai, anthropic } — openai 走 /v1/chat/completions（OpenAI 兼容）；anthropic 走 /v1/messages（Anthropic 原生，需配合对应 base_url）。默认 openai。",

    "_comment_env": "支持的环境变量：OPENCODE_BASE_URL / OPENCODE_API_KEY / LLM_MODEL / LLM_PROTOCOL",

    "protocol": "openai",

    "base_url": "https://api.minimaxi.com/anthropic",
    "api_key":  "sk-your-minimax-key-here",
    "model":    "MiniMax-M3",

    "_switch_to_anthropic_example": {
        "protocol": "anthropic",
        "base_url": "https://api.anthropic.com",
        "api_key":  "sk-ant-your-key",
        "model":    "claude-3-5-sonnet-20241022"
    }
}